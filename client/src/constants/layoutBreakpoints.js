/**
 * Min width (px) for persistent sidebar + desktop header layout.
 * Matches Tailwind `lg` (1024px). Phones in landscape often exceed `md` (768px) but stay below this.
 */
export const LAYOUT_DESKTOP_MIN_PX = 1024;
