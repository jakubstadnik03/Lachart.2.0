# Ignore peer-dependency conflicts from packages that haven't declared Capacitor 6 support yet
# (@codetrix-studio/capacitor-google-auth@3.3.6 still declares ^5, but works with Cap 6 at runtime)
legacy-peer-deps=true
